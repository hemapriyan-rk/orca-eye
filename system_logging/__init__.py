"""
ORCA EYE — System Logging Package
====================================
Modules:
  logger — per-frame JSONL + CSV metrics writer

Named 'system_logging' to avoid shadowing Python's built-in 'logging' module.
"""
